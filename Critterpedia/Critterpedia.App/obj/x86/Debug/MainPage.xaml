<Page
    x:Class="Critterpedia.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Critterpedia.App"
    xmlns:models="using:Critterpedia.App.Models"
    xmlns:viewmodels="using:Critterpedia.App.ViewModels"
    xmlns:uc="using:Critterpedia.App.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.BottomAppBar>
        <CommandBar>
            <!--<CommandBar.Content>
                <TextBlock Text="{x:Bind BugsViewModel.BugName, Mode=TwoWay}" 
                    FontSize="16" FontWeight="Light" Margin="10">
                </TextBlock>
            </CommandBar.Content>-->
            <!--<AppBarButton Icon="Save" Label="Save"
                          Command="{x:Bind NotesViewModel.SaveCommand}"
                          ToolTipService.ToolTip="Save Changes">
            </AppBarButton>-->
            <AppBarSeparator />
            <!--<AppBarButton Icon="Delete" Label="Delete"
                          Command="{x:Bind NotesViewModel.DeleteCommand}"
                          ToolTipService.ToolTip="Delete Note">
            </AppBarButton>-->
            <CommandBar.SecondaryCommands>
                <AppBarButton x:ConnectionId='2' x:Name="About" Label="About Critterpedia" ToolTipService.ToolTip="About Critterpedia Application"></AppBarButton>
                <AppBarButton x:ConnectionId='3' x:Name="Exit" Label="Exit" ToolTipService.ToolTip="Exit"></AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <uc:MasterDetailControl x:ConnectionId='4'
                x:Name="CritterSearchBox"
                Grid.Row="0" Grid.Column="0"
                                          />
        
        <!-- TODO: Add three image buttons, each button on click brings us to the master detail for that type of Critter. -->
    </Grid>
</Page>

